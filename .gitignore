# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Секреты (пароли) — не в git
.env

# База задач — только локально (на сервер / Антону передаём отдельно)
data/zadachi.db
data/*.db
# локальные автобэкапы — не в git
data/backups/
# экспорты Telegram (фото/видео) — не в git
data/imports/
# временные файлы sqlite
data/*.db-journal
data/*.db-wal
data/*.db-shm
# состояние ежемесячного Telegram-бэкапа
data/tg_monthly_state.json
data/tg_inbox_offset.json
data/backup_config.json

# OS / IDE
.DS_Store
.idea/
.vscode/

# deploy
deploy/
