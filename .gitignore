# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Секреты (пароли) — не в git
.env

# База задач хранится в git (data/zadachi.db)
# локальные автобэкапы — не в git
data/backups/
# экспорты Telegram (фото/видео) — не в git
data/imports/
# временные файлы sqlite
data/*.db-journal
data/*.db-wal
data/*.db-shm

# OS / IDE
.DS_Store
.idea/
.vscode/
